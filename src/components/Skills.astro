---
const lead = {
  label: 'Product & Growth',
  items: [
    'Product ownership',
    'Product strategy & roadmap',
    'Requirements & specs',
    'MVP & launch',
    'Prioritization',
    'Analytics & retention',
    'Monetization & pricing',
  ],
};

const groups = [
  {
    label: 'Delivery & Operations',
    items: [
      'Agile / Scrum',
      'Project & operations management',
      'Large-scale rollouts',
      'Risk management',
      'Stakeholder & vendor management',
      'Cross-functional & team leadership',
    ],
  },
  {
    label: 'Software & Tools',
    items: [
      'Swift / SwiftUI',
      'Python',
      'C++',
      'Xcode',
      'Figma',
      'KiCad',
      'AutoCAD',
      'Git & GitHub',
      'Firebase',
      'Excel',
      'MS Project',
    ],
  },
  {
    label: 'Electronics & Embedded',
    items: [
      'PCB design',
      'SMT',
      'MSP430',
      'Embedded (ESP32, Arduino, RPi)',
      'AVL / IoT',
      'Troubleshooting & repair',
    ],
  },
];
---

<section class="section" id="skills">
  <div class="wrap">
    <h2 class="eyebrow"><span class="num" aria-hidden="true">Log 04</span> Skills <span class="bar" aria-hidden="true"></span></h2>

    <div class="skills">
      <div class="sgroup sgroup--lead" data-reveal>
        <h3 class="sgroup__label">{lead.label}</h3>
        <ul class="chips">
          {lead.items.map((i) => <li class="tag">{i}</li>)}
        </ul>
      </div>

      {groups.map((g) => (
        <div class="sgroup" data-reveal>
          <h3 class="sgroup__label">{g.label}</h3>
          <ul class="chips">
            {g.items.map((i) => <li class="tag">{i}</li>)}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .skills {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: clamp(2rem, 5vw, 3.25rem) clamp(2rem, 5vw, 4rem);
  }
  .sgroup--lead { grid-column: 1 / -1; }
  .sgroup__label {
    font-size: 1.4rem;
    letter-spacing: -0.01em;
    padding-bottom: 0.9rem;
    border-bottom: var(--border-mid) solid var(--line-strong);
    margin-bottom: 1.25rem;
  }
  .chips {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  /* Skill chips are Tags. Long labels must wrap rather than overflow. */
  .chips .tag {
    white-space: normal;
    transition: border-color var(--dur-fast) var(--ease), color var(--dur-fast) var(--ease);
  }
  .chips .tag:hover { border-color: var(--line-strong); color: var(--ink); }
</style>
